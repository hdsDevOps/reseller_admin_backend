apiVersion: v1
kind: Service
metadata:
  name: adminbackend-voucherapi-service
spec:
  selector:
    app: adminbackend-voucherapi-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8008
